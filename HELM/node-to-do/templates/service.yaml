apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-to-do.fullname" . }}
  labels:
    {{- include "node-to-do.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "node-to-do.selectorLabels" . | nindent 4 }}
